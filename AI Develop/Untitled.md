① 每块开工前问一句：这块我要"懂"还是要"有"？
     懂 → 手写（但先写 oracle）
     有 → 委派（必须有 oracle）

② 没有 oracle 的 AI 产出，一律不接受。
   （oracle = 测试 / 金标 / 独立实现 / 回放数字）

③ AI 每犯一次错 → 加一行 AGENTS.md，或加一个脚本。
   （你的 AGENT.md 现在有 163 行，但它是"章程"；
     把它改成"错误账本"会立刻变有用）

④ 每个决策改动，记一行：这次是怎么发现的。
   （手敲 / 追问 / 测试 / 回放 / 审计）

⑤ 散文文档上限一页。其余的东西必须是机器能校验的。